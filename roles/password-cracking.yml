  # Password Cracking
  # Write app to crack in the cloud.
 
  - name: "Password cracking - Fedora-only packages"
    yum: name={{ item }} state=present
    with_items:
      - ncrack
      - john
      - ophcrack
      - cowpatty
      - pyrit
      - chntpw
    when: ansible_distribution == 'Fedora'
 
  - name: "Package: medusa"
    yum: name="medusa" state=present
  
  - name: "Package: sucrack"
    yum: name="sucrack" state=present
 
  - name: "Package: samdump2"
    yum: name="samdump2" state=present

  - name: "Hash Identifier"
    subversion: repo=http://hash-identifier.googlecode.com/svn/trunk/ dest={{ tools_root }}/password-cracking/hash-identifier

