# Hard Drive Fornsics
- name: "HDD Forensics packages"
  yum: name={{ item }} state=present
  with_items:
    - dc3dd
    - dcfldd
    - ddrescue
    - scalpel
    - testdisk
  when: ansible_os_family == "RedHat"

- name: "HDD forensics - Fedora only"
  yum: name={{ item }} state=present
  with_items:
    - extundelete
    - foremost
    - safecopy
    - recoverjpeg
  when: ansible_distribution == 'fedora'
