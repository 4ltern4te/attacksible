# IRC clients, chat etc

- name: "Package: weechat"
  yum: name=weechat state=present
  when: ansible_os_family == "RedHat"

- name: "Package: weechat"
  apt: name=weechat state=present
  when: ansible_os_family == "Debian"
